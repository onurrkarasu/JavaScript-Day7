/*
    ! Expression function
    expression functions are anonymous functions. after we create a function without a name and we assign it to a variable. to return a value from
    the function whe should call the variable. Look at the example below.
*/
    const square=function(n){
        return n*n;
    }
    console.log(square(2));